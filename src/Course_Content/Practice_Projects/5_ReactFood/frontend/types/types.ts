export type TMeal = {
  id: string;
  name: string;
  price: string;
  description: string;
  image: string;
};

export type TCartItem = {
  id: string;
  name: string;
  price: number;
  quantity: number;
}

