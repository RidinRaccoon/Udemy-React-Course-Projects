/**
 ** Expense related types
 */

type Expense = {
  id: string;
  title: string;
  amount: number;
  date: Date;
};

/**
 ** Chart related types
 */

type Point = {
  value: number;
  label: string;
};
